# YOLO Weights

Place the trained UAV detection model here and point `config.yaml` to it, for example:

```yaml
weights: weights/your_model.pt
```

Competition-trained `.pt` files are intentionally not included in the public repository because they are large and are separate from the source-code portfolio.
